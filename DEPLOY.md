# Deploy to Render - Manual Setup

## Quick Fix

In Render dashboard:

1. **Root Directory**: Set to `.`  (current root, not PO-Form)
2. **Build Command**: `npm install`
3. **Start Command**: `node server.js`

Or alternatively, we can move files up one level. Want me to do that?